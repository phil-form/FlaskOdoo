{#
    Corps du mail de réinitialisation.

    Un template Jinja peut produire autre chose que du HTML: ici du texte brut,
    rendu par render_template('emails/password_reset.txt', ...) depuis
    PasswordResetService. Écrire le mail dans un template plutôt que dans une
    chaîne Python permet de le relire et de le modifier sans toucher au code.

    Attention: pas de balise, donc aucun échappement HTML à craindre, mais
    n'insérez jamais de contenu non maîtrisé dans un mail non plus.
#}
Bonjour {{ username }},

Vous avez demandé la réinitialisation de votre mot de passe.

Pour choisir un nouveau mot de passe, ouvrez ce lien:

    {{ link }}

Ce lien est valable {{ minutes }} minutes et ne peut être utilisé qu'une seule
fois. Si vous n'êtes pas à l'origine de cette demande, ignorez ce message: votre
mot de passe actuel reste valable et personne n'a accès à votre compte.

--
L'équipe Flask MVC
